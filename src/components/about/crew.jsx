const crew = (props) => {
    return (
        <a href={props.instagram} target="_blank" rel="noopener noreferrer" className="font-din text-fourth text-lg block">{props.nama}</a>
    )
}

export default crew;